# CortexEx

Backend:
npm init -y
npm install express cors dotenv pg cookie-parser bcryptjs
npm install -D typescript ts-node-dev @types/node @types/express @types/cors @types/pg @types/cookie-parser @types/bcryptjs

Frontend:
npx create-next-app@latest